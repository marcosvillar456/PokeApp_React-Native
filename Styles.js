import { StyleSheet } from "react-native";

export default StyleSheet.create({
  heading: {
    fontSize: 32,
    fontWeight: "700",
  },
  container: {
    padding: 20,
  },
  row: {
    flexDirection: "row",
    alignItems: "center",
  },
});
